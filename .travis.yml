language: python
python:
  - "2.7"
sudo: false
install:
  - virtualenv .venv
  - . .venv/bin/activate
  - pip install flake8
  - pip install nose
script:
  - flake8 . --exclude=.venv --ignore=E501
  - nosetests
